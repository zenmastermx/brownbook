{
  "name": "brownbook-flutter",
  "version": "1.0.0",
  "description": "Brownbook Flutter web app",
  "scripts": {
    "predeploy": "flutter build web --release --web-renderer html",
    "deploy": "firebase deploy --only hosting"
  },
  "dependencies": {}
}
